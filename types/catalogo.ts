export interface CatalogoCelular {
  id: string;
  marca: string;
  modelo: string;
  colores: string[];
  almacenamientos: string[];
  rams: string[];
  imagen_url: string;
  descripcion: string;
  visible: boolean;
  created_at: string;
}
